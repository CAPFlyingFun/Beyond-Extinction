- [WebGL screenshot limitation](webgl-screenshot-limitation.md) — the automated screenshot tool's headless browser has no WebGL; Three.js apps show a boot-error fallback there but render fine in the real preview. Verify via tsc + vite build instead.
- [No-CDN constraint](no-cdn-constraint.md) — Beyond Extinction forbids external CDNs; fonts must be self-hosted via @fontsource (npm), not Google Fonts links.
- [Spec-fidelity for game tasks](spec-fidelity-game-tasks.md) — game task specs demand literal UX (named controls, exact log strings), not "cinematic equivalents"; match "Done looks like" verbatim.
- [Collision & spawn design](collision-spawn-prologue.md) — radius is baked into colliders, so raising it can trap spawns; keep required paths clear by authoring layout (move blockers off-lane, keep solid) not by toggling colliders; locomotion is weight-blended idle↔walk.
- [Character model pipeline](model-pipeline.md) — Meshy models come via GitHub Release (API can't pull), compress w/ gltf-transform; bump sw.js VERSION on asset swap; shared 24-bone rig = one clip library.
- [Google Drive downloads](google-drive-download.md) — curl/node recipe to pull large public Drive files server-side (no gdown/wget); "Sign-in" HTML page means file is still Restricted.
- [Exec-env secrets](exec-env-secrets.md) — project secrets are absent from the code_execution sandbox but present to the bash tool/workflows; use bash+node to use a key without exposing it.
- [Viewport camera framing](portrait-camera-framing.md) — fixed-FOV scenes dolly in via autoFramingScale(aspect) on both portrait AND wide-landscape phones; player FOV/distance prefs in Settings.ts compose multiplicatively (gameplay cams only).
- [GitHub push via connector](github-api-push.md) — git push is blocked for the main agent; push via connector token + Git Data API (bind per-project first, seed empty repo, mind replica lag, no delete_repo scope). Connector token CANNOT write .github/workflows/* (user adds by hand); multi-entry create-tree 404s — use Contents API per file.
- [Gesture overlay system](gesture-overlay-system.md) — shared ClipLibrary retargets clips by bone name; gesture weight has ONE owner (gestureBlend in applyLocomotion, never action.fadeIn/Out); strip Hips x/z root motion; drain pending promises on dispose.
- [Hand-rolled PWA](pwa-hand-rolled.md) — PWA is dependency-free (no vite-plugin-pwa) to keep frozen-lockfile CI deploy green; SW is base-path-agnostic, prod-only, caches gitignored models best-effort.
- [Portrait pipeline](portrait-pipeline.md) — portraits arrive via GitHub Release, are git-tracked, must be resized (huge 2813×5000 renders → x1600); base=Sarah.PNG (8wk lab), pregnancy/beach variants alongside; bump sw VERSION on swap.
- [Sarah idle hand-clip](sarah-idle-hand-clip.md) — her dialogue right-hand clip is Idle_4's arms-at-side rest pose + cup grip, NOT a two-idle/frame-0 blend bug; Idle_4 is already her calmest idle so swapping won't help.
- [Global overlay/settings disposal](global-overlay-disposal.md) — Overlays & settings panel are global; scenes must cancelChoice()+closeSettingsPanel() on dispose (director.cancel won't); Auto Play auto-runs ACTION gates, CHOICE always pauses.
